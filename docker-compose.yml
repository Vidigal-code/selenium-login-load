services:
  app:
    build: .
    container_name: selenium-login-load
    env_file:
      - .env
    volumes:
      - ./results:/app/results
    stdin_open: true
    tty: true
    command: >
      python -m source.main

networks:
  selenium-grid:
    driver: bridge

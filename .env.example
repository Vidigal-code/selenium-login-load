# PT: Valor mínimo permitido para logins simultâneos.
# EN: Minimum allowed simultaneous logins.
MIN_LOGINS=1

# PT: Valor máximo permitido para logins simultâneos.
# EN: Maximum allowed simultaneous logins.
MAX_LOGINS=1000

# PT: Tipo de sequência dos IDs dos logins: INCREASING, DECREASING ou RANDOM.
# EN: Sequence type for login IDs: INCREASING, DECREASING or RANDOM.
SEQUENCE=RANDOM

# PT: Quantidade de logins simultâneos (total de tentativas).
# EN: Number of simultaneous logins (total attempts).
CONCURRENT_LOGINS=50

# PT: Máximo de threads/processos concorrentes.
# EN: Maximum number of concurrent threads/processes.
MAX_CONCURRENT=10

# PT: Executa o navegador em modo headless (sem interface gráfica). Use 'true' para produção.
# EN: Run browser in headless mode (no GUI). Use 'true' for production.
HEADLESS=true

# PT: Tempo máximo para carregar uma página (em segundos).
# EN: Maximum page load timeout (seconds).
PAGE_LOAD_TIMEOUT=30

# PT: Tempo máximo para esperar elementos na página (em segundos).
# EN: Maximum wait for elements on page (seconds).
ELEMENT_WAIT_TIMEOUT=10

# PT: Usuário para login.
# EN: Login user.
LOGIN_USERNAME=tomsmith

# PT: Senha para login.
# EN: Login password.
LOGIN_PASSWORD=SuperSecretPassword!

# PT: Pasta principal para salvar resultados.
# EN: Main output folder for results.
OUTPUT_DIR=./results

# PT: Pasta para screenshots de cada login.
# EN: Folder for login screenshots.
OUTPUT_DIR_SCREENSHOT=./results/screenshot

# PT: Pasta para salvar HTML de cada login.
# EN: Folder for saving HTML per login.
OUTPUT_DIR_HTML=./results/html

# PT: Salva screenshots dos resultados dos logins (true/false).
# EN: Save screenshots of login results (true/false).
SAVE_SCREENSHOTS=true

# PT: Salva HTML da página após falha no login (true/false).
# EN: Save page HTML after failed login (true/false).
SAVE_HTML_ON_FAILURE=true

# PT: Exporta resultados em JSON (true/false).
# EN: Export results to JSON (true/false).
EXPORT_JSON=true

# PT: Exporta resultados em CSV (true/false).
# EN: Export results to CSV (true/false).
EXPORT_CSV=true

# PT: Modo de execução do Selenium ('local' para rodar na máquina ou 'grid' para usar Selenium Grid).
# EN: Selenium execution mode ('local' for local machine, 'grid' for Selenium Grid).
SELENIUM_MODE=local

# PT: URL do Selenium Hub (usado apenas no modo grid).
# EN: Selenium Hub URL (used only in grid mode).
SELENIUM_REMOTE_URL=http://selenium-hub:4444/wd/hub

# PT: URL da página de login (aplicação alvo).
# EN: Login page URL (target application).
TARGET_URL=https://the-internet.herokuapp.com/login

# PT: Fragmento da URL que indica sucesso do login (usado para validar redirecionamento).
# EN: URL fragment indicating login success (used for redirect validation).
TARGET_URL_TO_CHECK=/secure

# PT: Habilita/desabilita uso do Selenium Grid (true/false).
# EN: Enable/disable Selenium Grid (true/false).
USE_GRID=false

# PT: Quantidade de nodes Chrome no Grid (usado no docker-compose).
# EN: Number of Chrome nodes in Grid (used in docker-compose).
GRID_NODES=3

# PT: Caminho do ChromeDriver local (necessário para execução local no Windows).
# EN: Local ChromeDriver path (required for Windows local execution).
# Docker Config or windows C:\WebDriver\bin\chromedriver.exe
CHROMEDRIVER_PATH=/usr/bin/chromedriver
